const autoprefixer = require('autoprefixer');

module.exports = {
  entry: {
    "bundle-dev": ['./app.scss', './app.js'],
    "bundle": ['./app.scss', './app-blazor.js'],
  },
  output: {
    filename: 'material-[name].js',
  },
  optimization: {
    minimize: true
  },
  module: {
    rules: [
      {
        test: /\.scss$/,
        use: [
          {
            loader: 'file-loader',
            options: {
              name: 'material-bundle.css',
            },
          },
          {loader: 'extract-loader'},
          {loader: 'css-loader'},
          {
            loader: 'postcss-loader',
            options: {
              plugins: () => [autoprefixer()]
            }
          },
          {
            loader: 'sass-loader',
            options: {
              sassOptions: {
                includePaths: ['./node_modules'],
              }
            },
          }
        ],
      },
      {
        test: /\.js$/,
        loader: 'babel-loader',
        query: {
          presets: ['@babel/preset-env'],
        },
      }
    ],
  },
};